# Cute Badge Background Studio v1.8

293×164 배지/썸네일 배경 제작용 정적 웹앱입니다.

## v1.8 수정
- 내부 포인트 효과의 `첫 줄 블러`가 실제로 보이도록 렌더링 로직 수정
- 지정한 방향의 시작 영역은 **블러 처리된 패턴**이 보이고,
  그 뒤로 갈수록 **선명한 패턴으로 자연스럽게 전환**되도록 변경
- `첫 줄 블러 강도`와 `블러/페이드 폭`을 함께 조절 가능
- 왼쪽 / 오른쪽 / 위 / 아래 방향 모두 적용

## 실행
압축 해제 후 `index.html`을 열거나 GitHub Pages / Cloudflare Pages에 그대로 업로드하면 됩니다.
